name: easy-agent
version: 0.1.0
description: APM project for easy-agent
author: tamara1031
dependencies:
  apm:
    - git: https://github.com/tamara1031/easy-agents
      path: easy-agent
      ref: main
  mcp: []
scripts:
  install: apm install --no-policy --target copilot,claude
  compile: apm compile --target copilot,claude
